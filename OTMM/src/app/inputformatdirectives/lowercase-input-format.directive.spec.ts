import { LowercaseInputFormatDirective } from './lowercase-input-format.directive';

describe('LowercaseInputFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new LowercaseInputFormatDirective();
    expect(directive).toBeTruthy();
  });
});
